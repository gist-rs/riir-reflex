# 04_agent_skill — the reflex-integration skill

| File | What it covers |
|---|---|
| `SKILL.md` | The `reflex-integration` agent skill — install the binary, ask typed questions over the HTTP wire, wire abstention into a cascade, fit confidence thresholds. This copy is the SOURCE OF TRUTH; the arena site serves a mirror (`skills/reflex-integration/SKILL.md` in `../reflex-site`) — after editing, run `python3 ../reflex-site/scripts/sync_mirror.py` and commit both repos |
