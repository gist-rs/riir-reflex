# Issue 017 — ANE lane reference design (laya-apple distill; owner hint 2026-09-24)

**Status:** OPEN — reference design distilled from tc3oliver/laya-apple v1.0; **gate 1 RATIFIED at feasibility+bench scope 2026-09-24** (owner directive in-session; **release scope RULED: download-on-demand, digest-pinned** — binds at the serve wiring); **Phase 0 COMPLETE 2026-09-24** (Plan 002 P0: six BC1S FP16 artifacts, 100%-ANE / 0 transitions each, smoke 24/24 top-1); **P1 COMPLETE 2026-09-24** (Plan 002 T1.1–T1.4: substrate runtime riir-infer `78a91c3`+`5c8ec2a` — digest verify, content-addressed compile cache, LOAD-time MLComputePlan re-gate; `RiirAgent::load_ane` constructor-selects, env-only ANE refused; G5-ANE gate GREEN 76/76 top-1 = 1.000, zero flips, structural parity exact; serialized timing AC/powermode-2 preflight: ANE ~1.5× Metal on english+typed, p50-parity on multilingual with the ANE tail win; **the ml 0.0200 question ANSWERED at 0.0264 full-corpus — above the 0.02 class, decision gate still 100%**); **P2 COMPLETE 2026-09-24** (the bench publish landed inside Issue 023 T5: reflex-site `3ab373f`, worker `cde063c2`, ANE device column live). Remaining: the serve wiring (routing + no-silent-fallback + download-on-demand).

**Source pin:** `tc3oliver/laya-apple` @ `128a19302c616173617d1d8d4b152e2f667c618c` (Apache-2.0), benchmarks/v1.0.md + docs/support-matrix.md + docs/no-silent-fallback.md. Measured on **Apple M4 Max / macOS 26.6.2 / coremltools 9.0 / MLX 0.32.2** — their own routing.json says "thresholds are not validated elsewhere". Distilled 2026-09-24 (riir-clippy distill verdict: MARGINAL C corpus / LANE INTEL HIGH).

## Why an ANE lane is plausible for reflex

The owner hint: "reflex may need ane lane btw". laya-apple v1.0 is the first production-qualified ANE configuration for THIS exact model family (Laya decisions), and its shape fits the reflex serve edge precisely:

- **ANE wins the short single-question lane decisively** (M4 Max, forward P50): laya L64 8.1 ms / L128 9.9 ms; laya-multilingual L64 **3.4 ms** / L128 4.3 ms / L256 8.3 ms; typed L64 8.0 ms. At those lengths ANE beats even MLX FP16 GPU (9.4 / 5.4 ms at L64) and PyTorch MPS (21.4 / 16.7 ms). Reflex requests are single-decision, short (fixture p50 ~100 tokens) — exactly the winning class.
- **Heterogeneous GPU+ANE serving** (their closed-loop mix): 2.92–4.57× throughput vs GPU-only, short-stream P99 36–84 ms → 7–12 ms, head-of-line blocking removed (a long GPU forward no longer delays short requests). Their open-loop bursty rows are starker: short P50 195 ms GPU-only → 10.6 ms heterogeneous.
- **What an ANE lane does NOT replace**: L512+ (bucket sets top out at L128 for laya/typed, L128 for multilingual auto-routing — L256 parity passes but loses the conservative routing comparison) and multi-question requests (MLX/GPU batch wins at every measured length — never route those to ANE).

## The qualified configuration (what MUST be copied, not approximated)

1. **BC1S graphs only** — fixed batch=1, fixed length buckets (they convert per-bucket, `bc1s-masked`). The "ordinary" CoreML graph (BxLxC/SDPA, flexible/enumerated shapes) is **numerically WRONG on the ANE** — up to 85/187 hard decision mismatches, max prob error 1.0 — AND at L1024 a compile failure **silently drops to CPU** (2201.8 ms). Flexible/enumerated graphs run **100% on CPU** on this OS under every compute-unit setting. An ANE lane ships fixed-shape buckets or it ships garbage.
2. **Compute-plan verification at load**: 100% of ops on the Neural Engine + 0 device transitions, checked at build/import/load and after any change — else reject (`ComputeUnitMismatchError`), plus a RUNTIME placement probe (the static plan cannot see runtime placement — their audit finding V1).
3. **No silent fallback** (their doctrine, worth adopting verbatim): the router decides BEFORE the request runs and records the reason (`routing_reason`); a failure on a device stays on that device and fails the request; a corrupt/failed ANE artifact is dropped with a `RuntimeWarning` and its requests go to the other device WITH the reason recorded; an explicit-device request never pads up to a larger bucket (`UnsupportedShapeError` instead).
4. **Conservative auto-routing derivation** (derive_routing.py): walking buckets ascending, bucket b_i joins iff (a) BC1S parity passed at b_i AND (b) ANE P50 at b_i < MLX P50 at **b_{i-1}** (padding-cost-aware: a request just above b_{i-1} pads to b_i on ANE but costs ~MLX(b_{i-1}) on GPU); stop at the first failure. Note their own measured crossover ≠ production threshold (multilingual L256 ANE 8.3 ms is actually faster than MLX-same-length 8.6 ms, but loses to MLX at the previous bucket — the rule is deliberately conservative).
5. **Parity contract is DECISION-level, not p-drift** (load-bearing for our G5): their FP16 gate = prob error ≤ 0.02 + 0 hard mismatches + all finite + bitwise-identical repeats. A **hard mismatch** = argmax differs AND the reference's own top1–top2 calibrated margin ≥ 2× tolerance. A **near-tie flip** = decision differs inside that band — listed row by row, never hidden, never counted as a failure. Their ANE FP16 measured max prob err 0.0077–0.0128 — an ANE arm will very likely FAIL our Metal-lane G5 bar (p-drift ≤ 1e-3), so the ANE arm needs its OWN gate: top-1 agreement + near-tie-band report + a decision-level bar in the 0.02 class (calibrated on OUR goldens, not copied).

## NOT transferable

- **Absolute milliseconds** — M4 Max vs our M3 Max, their MLX FP16 GPU vs our hand-written MSL kernels. Same-box league cells own comparisons; our ANE numbers must be measured fresh on the M3.
- **The Python runtime/conversion tooling** — owner directive: no Python anywhere in the laya lane. Consistent split: coremltools conversion is a ONE-TIME OFFLINE step (like GGUF export); the shipped artifact is a compiled `.mlpackage`; the RUNTIME is native Rust. Python never runs at serving time. **The carve-out names TWO offline sites (Claude verdict 2026-09-24 r1 — the law as first written named only one):** (1) the conversion helper `scripts/ane_convert.py`; (2) the bench publish `../reflex-site/scripts/publish_bench.py` (Plan 002 T2.2's sanitize step). Both one-time/offline, neither a build dep, neither on the serving or bench-runtime path.
- **MLX** — we do not use it; our GPU lane stays the Metal kernels.

## Reference implementation path (Rust)

- Feature `laya-riir-ane` (opt-in, macOS-only, native-only — never joins a wasm32 combo; per repo law, lands in the SAME commit as its parity `[[test]]` row).
- Deps: `objc2` (already in the Metal lane) + `objc2-core-ml` (+ `objc2-foundation`); MLModel load, fixed-shape `MLArrayBatchProvider`/`MLFeatureValue` FP16 inputs, prediction → logits → the existing calibration head unchanged.
- Artifacts: `assets/ane/` (or `LAYA_ANE_ARTIFACTS_DIR`) per model × bucket `.mlpackage`, produced by an offline `scripts/` conversion helper; digest-pinned in the manifest (the weights-hash discipline this repo already has).
- Load-time: compute-plan 100%-ANE/0-transitions verify → refuse otherwise; bucket → shape table; explicit-device request outside buckets = loud `Unsupported` (never pad up).
- Serve routing (serve.rs): single-question + padded-len ≤ max bucket + ANE healthy → ANE (reason recorded); everything else → Metal/CPU unchanged; ANE artifact/runtime failure → loud one-time warning + recorded-reason fallback (their rule 6/14), never a silent swap.
- Bench: `laya_fixture_timing --device ane` posture vs Metal vs CPU, SERIALIZED (Issue 015 containment — no parallel Metal/ANE instances in the same process until 015's root cause is diagnosed); A/B against the current Metal numbers, position-balanced rounds.

## Tasks

- [x] Owner gate 1: is an ANE lane wanted at all (new artifact class `.mlpackage` in release archives vs download-on-demand; the no-Python boundary formally ratified for the offline-conversion step)? — **RATIFIED 2026-09-24 at the feasibility + bench scope** (owner directive in-session: "add plan to add ane lane bench to reflex.gist.rs/bench and do that first"): Plan 002 exists, the offline-conversion boundary holds (`scripts/ane_convert.py`, never at serving time). Release scope RULED 2026-09-24 (Claude verdict, request_verdict session ce4289b5 r1): **download-on-demand, digest-pinned** — `LAYA_ANE_ARTIFACTS_DIR` + fetch-on-first-use under the BLAKE3 manifest pin (macOS-only artifacts must not bloat the 5-target release archives; the digest-pin discipline already owns the manifest). Binds at P3 (serve wiring); the bench (P0/P1/P2) never needed it.
- [x] **Adjudication (precondition to the P1 runtime substrate home) — RESOLVED 2026-09-24 (Claude verdict r1 + code read): `ane_prefill` vs this lane — SITS BESIDE, does not join.** `riir-infer-gpu/src/ane_prefill/` is a per-op ANE **program-bank** executor for GDN ternary decoder projections (`in_proj_concat`/`gate_up_proj`, ≥2048-token prefill, Form C int8 requant, 96-program compiled bank, hidden-state-cosine G1) — different model family, granularity, runtime API, and numerics discipline from this lane's whole-graph CoreML `.mlpackage` **encoder** (ModernBERT, BC1S FP16, decision-level parity). The apparent doctrine collision reconciles by axis: `ane_prefill` is fail-open on the **compute path** (a perf accelerator — the GPU computes the same answer when ANE declines; the answer's MEANING never changes), this lane is no-silent-fallback on the **reporting path** (a device is part of the request/bench contract — an ANE failure is a FAILED device row, never a CPU number wearing an ANE label). No coupling now; a shared ANE substrate (context/device discovery, headroom) MAY be lifted later on evidence. This is what makes the substrate home well-formed: the runtime lands in riir-infer BESIDE `ane_prefill` (after T4 moves `src/laya` there), with the serve router's recorded-reason semantics owning the reporting path. (Substrate-first lesson on record: neither 017 nor Plan 002 named the shipped executor — vocabulary translation, not single-vocabulary grep.)
- [x] Phase 0 feasibility: offline coremltools conversion of laya + laya-multilingual (BC1S FP16, buckets {64,96,128} + {256} ml), compute-plan verify passes on the M3. — **DONE 2026-09-24 (Plan 002 P0, commits `de3de3e`+`53df6e1`)**: all THREE models (multilingual, english, typed) at buckets {64, 128} — the bench scope; 96/{256}ml ride later. Six artifacts, every one 100%-ANE / 0 transitions (ml L64 1373/1373, L128 1387/1387; en/td L64 1745/1745, L128 1763/1763) — the gate proven live by REFUSING the first shape (embedding gather; moved host-side). Numerical smoke vs the frozen G5 goldens: 24/24 top-1, max prob err ml 0.0200 / en 0.0077 / td 0.0146 (OBSERVED, never gated; ml's 0.0200 sits above the reference class — recorded for the P1 re-bench), zero flips. Artifacts local-only (gitignored, BLAKE3 digest-pinned manifest) per the download-on-demand ruling. Notes: the embedding gather never places on the ANE — P1's runtime gets host-gathered fp16 inputs (the table is resident anyway); Python 3.12 is load-bearing for the compute-plan bindings (3.14 loads blind).
- [x] `laya-riir-ane` feature + objc2-core-ml runtime path (same commit as the gate row). — **DONE 2026-09-24 (Plan 002 P1; riir-infer `78a91c3`+`5c8ec2a`, reflex gate row in the same commit train)**: blake3-dir digest verify → content-addressed compile cache (staging + atomic claim — the shared-fixed-path lesson mechanized) → `CPUAndNeuralEngine` load → **MLComputePlan gate RE-RUN at load** (100% device ops ANE + 0 transitions; the conversion-time record alone would trust the artifact forever — the load-time walk refuses when the OS re-plans; count drift disclosed, verdict-is-the-gate). fp16 host gather (pad-token tail fill, the smoke's exact construction) + manifest-sentinel pad_bias → fp16 out sliced [:n] → bit-exact f32 widen → the UNCHANGED f32 head. `RiirAgent::load_ane` CONSTRUCTOR-selects the posture — an env value can never demote an explicitly requested lane; `LAYA_DEVICE=ane` through plain `load()` is REFUSED loud.
- [x] BOUNDARY.md allowlist row for the new deps — `objc2-core-ml` + `objc2-foundation` (the allowlist currently carries `metal` + `objc2` only); rides the SAME feature commit per the boundary contract. — **DONE** (riir-infer BOUNDARY.md, macOS target-scoped row; block2 0.6 named — the ObjC block runtime the no-copy inputs, the output reader and the async plan loader need).
- [x] G5-ANE parity gate: top-1 agreement vs our frozen goldens + near-tie-band report + decision-level prob-err bar calibrated on OUR fixtures; p-drift published as OBSERVED, never gated at 1e-3. — **DONE 2026-09-24, GREEN first run** (`tests/laya_ane_parity.rs` + its `[[test]]` row): 76/76 in-bucket top-1 = 1.000000 (en 22, td 22, ml 32), ZERO flips (zero near-tie, zero hard), structural parity exact ×76, 12 skips named + floored. Max prob err en 0.0077 / td 0.0146 / **ml 0.0264 — the ml 0.0200 question ANSWERED: exceeds the 0.02 class at full-corpus scale and the decision-level gate still holds at 100%** (the gate design vindicated: decisions, not drift). Serialized timing (AC/powermode-2 preflight, position-balanced): ANE ~1.5× Metal on english+typed, p50-parity on multilingual with the ANE tail win — Plan 002's T1.3 table.
- [x] `laya_fixture_timing --device ane` posture + serialized A/B vs Metal on the M3; record absolute numbers + the box state. — **DONE 2026-09-24** (Plan 002 T1.3): serialized one-device-per-process, position-balanced (ANE-first 5 reps, Metal-first 3 reps), preflight `PROVENANCE: power=AC Power load=4.23 swap=2747.94M canary=148.2us/best5 powermode=2(high)`. ANE ~1.5× Metal on english (24.5/25.5 vs 41.4/39.3 ms p50) + typed (25.5/22.6 vs 41.5/33.2), p50-parity on multilingual with the ANE tail win (round-2 p90 15.9 vs 18.2). Out-of-bucket rows (n > L128) refused loudly before warmup — means are NOT row-set-identical, p50 is the comparable statistic.
- [x] **P2 (the bench publish) DONE 2026-09-24** — landed inside the Issue 023 T5 together-republish (reflex-site `3ab373f`, worker `cde063c2`): the ANE device column (m3-ane host) + fresh m3 metal primary live on reflex.gist.rs/bench, massive 0.69 across all three hosts; the ANE device-row FAQ `a67d2a1`. Plan 002 CLOSED.
- [ ] serve.rs routing + no-silent-fallback wiring (reasons recorded, explicit-device refusal outside buckets) + the download-on-demand artifact fetch (owner ruling: `LAYA_ANE_ARTIFACTS_DIR` + fetch-on-first-use under the BLAKE3 manifest pin — binds here).
- [-] defer heterogeneous ANE+Metal two-queue serving (the 2.9–4.6× class) until Issue 015's parallel-instance root cause is diagnosed and the site's single-instance serialized posture has a plan — record as deferred, not dropped.
- [-] defer laya-typed-decisions ANE conversion until laya + multilingual are green (same ModernBERT-large geometry; no new information expected from a third model early).
